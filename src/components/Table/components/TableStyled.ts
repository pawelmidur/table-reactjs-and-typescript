import styled from "styled-components";

export const TableStyled = styled.table`
  table-layout: auto;
  width: 100%;
  display: table;
  border-spacing: 0;
  border-collapse: collapse;
  overflow: scroll;
`;